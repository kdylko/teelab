 <%= form_for(@order, :html => { :style => 'display:none;' }) do |f|  %>



    <div class="field">
      <%= f.hidden_field :pay, value: "paid" %>
    </div>

        <div class="field">
      <%= f.submit 'Pay' %>
    </div>

 <% end %>